var pigLatin = function(input) {

  if (input.charAt(0).match(/^(a|e|i|o|u)/)) {
    var input = input.concat("ay");
    return(input);
    console.log(input);
  } else if (!input.match(/^(a|e|i|o|u)/)) {
      for (var i = 0 ; i < inputArray.length; i++) {
        inputArray[0].match(/^(a|e|i|o|u)/) {

        } else {
          var input = input.concat("ay");
          return(input)
        }
      }
  } else {
    return(input);
  }
};



/* split string into array with .split() */


/*chartAt(0).match(/^(a|e|i|o|u)/)) {
    var input = input.concat("ay");
    return(input);
  } else if (!charAt(0).match(/^(a|e|i|o|u)/)) {
    input.forEach(function(input) {
});








*/

// var vowels = (a,e,i,o,u)
// var consonants = (b,c,d,f,g,h,j,k,l,m,n,o,p,q,r,s,t,v,w,x,y,z)
// var input =  {
//
//   if (input.charAt(0).match(vowels) {
//     var output = input.concat("ay");
//     return prompt(output);
//     console.log(input);
//   } else if index.forEach(consonants)
//     return prompt("this has a consonant")
